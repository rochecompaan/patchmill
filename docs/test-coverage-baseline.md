# Test Coverage Baseline

Generated with:

```bash
npm run test:coverage
```

## Summary

- Date: 2026-05-24
- Runner: Node built-in `node:test` coverage
- Line coverage: 94.24%
- Branch coverage: about 84.4% (84.37%-84.41% observed across repeated local
  runs)
- Function coverage: 96.33%

## Initial observations

- `scripts/agent-issue/pipeline.test.ts` is the largest test hotspot by size and
  should be reviewed first for duplicate orchestration coverage.
- Direct unit tests should remain close to the modules they exercise.
- Pipeline tests should be retained when they cover unique workflow transitions,
  resume behavior, or safety boundaries not asserted elsewhere.
- No coverage threshold is enforced yet; branch coverage varied slightly across
  repeated full-suite runs (84.37%-84.41%), so thresholds should be considered
  after the cleanup pass stabilizes the baseline.

## First audit targets

1. `scripts/agent-issue/pipeline.test.ts` — identify scenarios that retest
   lower-level selection, state, prompt, git, or visual-evidence behavior.
2. `scripts/agent-issue/pi.test.ts` and `scripts/agent-issue/prompts.test.ts` —
   check for duplicated prompt-string assertions and replace brittle text checks
   with contract-level assertions where possible.
3. `src/config/load.test.ts` — check whether validation cases can be
   table-driven while preserving one direct assertion per validation rule.

## Candidate cleanup items

Legend:

- `keep`: retain for unique orchestration, resume, safety, or side-effect
  sequencing.
- `merge`: duplicate or near-duplicate variation coverage; consolidate to fewer
  assertions.
- `move-down`: move detailed behavioral checks to lower-level tests.
- `delete-after-confirming-direct-coverage`: delete only after direct tests
  protect the same behavior.

| File                                   | Test or area                                                                                                                                                                                                                                                                                                                                                                                                                 | Recommendation                            | Reason                                                                                                                                                                                                                                   | Required safety check                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| -------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `scripts/agent-issue/pipeline.test.ts` | Dry-run preview cluster: `runOneIssue dry-run lists open issues and returns the selected agent-ready issue without mutations`, `runOneIssue dry-run ignores resumable in-progress issues and previews the next ready issue`, and `runOneIssue dry-run previews agent-ready issues even when saved state has stale finished branch and worktree`                                                                              | `merge`                                   | These all exercise the same dry-run listing path. Keep one pipeline smoke for end-to-end preview behavior, but push ranking and eligibility detail back to direct selection/state tests.                                                 | `scripts/agent-issue/selection.test.ts` (`selectIssue chooses the highest-priority agent-ready issue`, `selectIssue uses triage policy ready label, protection labels, and priority ordering`) plus `scripts/agent-issue/run-state.test.ts` (`isResumableRunState accepts active automation phases only`) and `npm test`                                                                                                                                                                                                                 |
| `scripts/agent-issue/pipeline.test.ts` | `runOneIssue renders configured project policy visual evidence fields in the implementation prompt`                                                                                                                                                                                                                                                                                                                          | `move-down`                               | Most assertions are prompt-string contract checks, which are easier to maintain in direct prompt tests than in a full orchestration run. A smaller pipeline smoke can still prove the prompt reaches Pi.                                 | `scripts/agent-issue/prompts.test.ts` (`buildImplementationPrompt renders structured visual evidence policy fields`, `policy-driven prompts render validation and landing contract text from runtime inputs`, `buildImplementationPrompt renders configured skills`) and `npm test`                                                                                                                                                                                                                                                      |
| `scripts/agent-issue/pipeline.test.ts` | `runOneIssue uses the configured worktree strategy for workspace names and prompt instructions`                                                                                                                                                                                                                                                                                                                              | `merge`                                   | This test mixes branch/worktree naming, git worktree creation, Pi cwd, and prompt copy. Keep the orchestration check that the configured worktree is used, but move the detailed naming and prompt-text assertions to lower-level tests. | `src/git/worktree-strategy.test.ts`, `scripts/agent-issue/git.test.ts` (`createIssueWorktree accepts a configured worktree strategy`), `scripts/agent-issue/prompts.test.ts` (`buildImplementationPrompt uses configured direct-land policy inputs`), and `npm test`                                                                                                                                                                                                                                                                     |
| `scripts/agent-issue/pipeline.test.ts` | Clean-worktree ignore cluster: `runOneIssue ignores configured run-state logs during the clean-worktree check`, `runOneIssue ignores the default Pi todo root during the clean-worktree check`, `runOneIssue ignores a custom Pi todo root during the clean-worktree check`, `runOneIssue ignores a custom Pi todo root when reusing an existing worktree`, and `runOneIssue honors configured clean-status ignore prefixes` | `merge`                                   | All five scenarios drive the same preflight cleanliness branch with different ignored paths. One pipeline case should be enough once direct git tests continue covering ignore-prefix behavior.                                          | `scripts/agent-issue/git.test.ts` (`assertCleanWorktree ignores configured run-state logs`, `assertCleanWorktree ignores configured todo roots`, `ensureIssueWorktree ignores configured todo roots when reusing an existing worktree`, `ensureIssueWorktree ignores porcelain entries for similar absolute paths`) and `npm test`                                                                                                                                                                                                       |
| `scripts/agent-issue/pipeline.test.ts` | Cleanup-hook trio: `runOneIssue does not run cleanup commands when cleanup hooks are not configured`, `runOneIssue runs configured generic cleanup hooks`, and `runOneIssue reports cleanup hook failures when process termination is unsafe`                                                                                                                                                                                | `merge`                                   | The hook mechanics already have direct coverage; the pipeline only needs one representative resume/handoff integration case to prove cleanup is invoked from `runOneIssue`.                                                              | `src/cleanup/hooks.test.ts` (`runCleanupHooks is a no-op when no cleanup hooks are configured`, `runCleanupHooks runs generic command hooks from the worktree root`, `runCleanupHooks reports process cleanup failures with hook context`) and `npm test`                                                                                                                                                                                                                                                                                |
| `scripts/agent-issue/pipeline.test.ts` | `runOneIssue implementation heartbeat reads task progress from the issue worktree`                                                                                                                                                                                                                                                                                                                                           | `move-down`                               | The core behavior is task-progress discovery, which belongs closer to Pi/task readers than to the full issue pipeline. The pipeline assertion mainly waits for a heartbeat tick.                                                         | `scripts/agent-issue/pi.test.ts` (`runPiPrompt reads issue task progress from the configured worktree root`), `scripts/agent-issue/issue-todos.test.ts` (`readIssueTodoTasks returns sorted implementation task labels`), and `npm test`                                                                                                                                                                                                                                                                                                 |
| `scripts/agent-issue/pipeline.test.ts` | Progress-rendering pair: `runOneIssue emits visible implementation subtask step labels` and `runOneIssue moves streamed tool calls under the active implementation task`                                                                                                                                                                                                                                                     | `merge`                                   | These are largely reporter-shape assertions. Keep at most one end-to-end wiring smoke and rely on direct console-progress tests for the detailed step-label and tool-call formatting rules.                                              | `scripts/agent-issue/console-progress.test.ts` (`console reporter renders numbered steps with tool-call summaries and output token summaries`, `console reporter renders mandatory implementation task labels`, `console reporter writes tool-call summaries as observations arrive`) and `npm test`                                                                                                                                                                                                                                     |
| `scripts/agent-issue/pipeline.test.ts` | `runOneIssue uploads visual evidence to the PR before posting the issue handoff`                                                                                                                                                                                                                                                                                                                                             | `keep`                                    | This is a true orchestration boundary: the important assertion is ordering between upload side effects and the handoff comment, and there is no lower-level test that protects that sequence.                                            | Keep this pipeline scenario in `npm test`; supporting lower-level checks remain in `scripts/agent-issue/visual-evidence.test.ts` and `src/host/forgejo-visual-evidence.test.ts`                                                                                                                                                                                                                                                                                                                                                          |
| `scripts/agent-issue/pipeline.test.ts` | `runOneIssue keeps visual evidence when no uploader is configured`                                                                                                                                                                                                                                                                                                                                                           | `delete-after-confirming-direct-coverage` | Once the upload-order pipeline test stays in place, this full pipeline pass-through case appears redundant with the direct helper coverage for the no-uploader branch.                                                                   | `scripts/agent-issue/visual-evidence.test.ts` (`uploadPrVisualEvidence keeps evidence when no uploader is configured`) plus `npm test` and `npm run test:coverage`                                                                                                                                                                                                                                                                                                                                                                       |
| `scripts/agent-issue/prompts.test.ts`  | Broad prompt-contract pair: `buildPlanCreationPrompt includes issue context, workflow rules, and result contracts` and `buildImplementationPrompt includes plan-first execution, review loop, validation rules, and result contracts`                                                                                                                                                                                        | `merge`                                   | These are large kitchen-sink prompt assertions, while many focused tests in the same file already cover labels, skills, direct-land policy, visual evidence, resume context, and task-contract overrides.                                | `scripts/agent-issue/prompts.test.ts` (`buildPlanCreationPrompt renders configured ready and needs-info labels`, `buildImplementationPrompt renders configured skills`, `buildImplementationPrompt renders structured visual evidence policy fields`, `policy-driven prompts render validation and landing contract text from runtime inputs`, `buildImplementationPrompt includes resume context when resuming existing work`, and `task contract overrides drive todo instructions in plan and implementation prompts`) and `npm test` |
| `src/config/load.test.ts`              | Invalid-config validation matrix from `loadPatchmillConfig reports invalid config field types` through `loadPatchmillConfig reports invalid visual evidence example types`                                                                                                                                                                                                                                                   | `merge`                                   | The negative coverage looks intentionally broad, but the cases are table-shaped and could be consolidated without losing one direct assertion per validation rule.                                                                       | Preserve one table row per rule and keep `npm test` as the command-level guard                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

## Cleanup pass 1 results

- Removed two redundant dry-run pipeline scenarios while keeping the
  representative dry-run smoke test.
- Direct safety coverage remains in `scripts/agent-issue/selection.test.ts` and
  `scripts/agent-issue/run-state.test.ts`.
- Removed two clean-worktree ignore pipeline variants while keeping the
  configured clean-status ignore prefix smoke test.
- Direct safety coverage remains in `scripts/agent-issue/git.test.ts` for
  run-state log, todo-root, reuse, and similar-path ignore behavior.
